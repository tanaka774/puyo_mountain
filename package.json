{
  "name": "puyo_mountain",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx vite build && wrangler dev",
    "deploy_dev": "npx vite build && npx wrangler deploy",
    "deploy_prod": "npx vite build && npx wrangler deploy --env production"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^4.5.14"
  },
  "dependencies": {
    "@types/grecaptcha": "^3.0.4",
    "@types/recaptcha2": "^1.3.1",
    "@vercel/node": "^2.15.5",
    "@vercel/postgres": "^0.4.0",
    "phaser": "^3.88.2",
    "vercel": "^35.2.3"
  },
  "type": "module"
}
